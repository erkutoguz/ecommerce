# Development-only prerequisite data. The INSERT is idempotent and does not
# overwrite runtime stock changes after a container restart.
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:dev/data.sql
