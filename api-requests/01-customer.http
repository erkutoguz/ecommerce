### Step 1 - Read the provisioned Customer
# Requires: Auth.http registration, login, and at least one provisioning poll.
# Produces: customerId, which the cart flow uses.

GET {{baseUrl}}/customers/{{customerId}}
Authorization: Bearer {{accessToken}}
